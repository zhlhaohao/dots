# CONTEXT

本项目的领域词汇表。实现细节见代码与 CLAUDE.md，此处只记语言。

## 术语

- **JSBridge（桥）**：WebView 网页脚本与原生 App 之间的通信框架。网页侧通过注入对象 `Android` 发起调用，原生侧按函数名分发给插件，处理结果以回调形式送回网页。
- **插件（Plugin）**：实现某个具体桥函数的原生处理器。异步插件继承 `BaseJSPlugin`，同步插件继承 `BaseJSPluginSync`。
- **异步通道**：网页调用后不阻塞，原生在 UI 线程处理，完成后经四态回调之一答复。适合需要回调、UI 操作或多步业务的场景。
- **同步通道**：网页调用立即拿到返回值（JSON 字符串），原生在 JS 线程处理，只允许纯数据查询，禁止触碰 UI。
- **四态回调**：异步通道答复的四种状态——成功（success）/ 失败（fail）/ 用户取消（cancel）/ 完成（completion）。每个异步请求恰好答复一次。
- **回调字典**：网页侧保存 callbackId → 四态闭包映射的表；回调送达即消费并删除。调用未注册函数或插件抛异常时，框架以 fail 态兜底答复。
- **宿主（Host）**：承载 WebView 并注册插件的页面。本项目中为 `WebViewActivity`；插件经 `WebViewHost` 接口访问宿主能力。
- **容器标识（UA 标识）**：追加在 WebView UserAgent 末尾的 `NexBox/主版本.次版本` 标记。网页据此感知「运行在本容器内」及容器版本。
- **演示页**：内置的本地网页（`assets/webpage/jsbridge_demo.html`），逐按钮验证桥接口的行为。
- **格子网**：Dashboard 上的启动格子网，「九宫格」为扩容前旧名（历史文档沿用）。4 列纵向滚动；条目数上限 20（5 整行）；每格对应一个网页入口，单击即在宿主页面打开该格的 URL。演示页是出厂格子网中的一格。首屏不足 12 格时补齐空格子（4×3 观感）。
- **格子条目**：格子网中的一格，含标题与 URL；条目可为空。
- **空格子**：无标题无 URL 的占位格子。不响应单击，仅保证网格形状完整；长按可编辑填充。
- **出厂配置**：随安装包内置的九宫格初始布局。首次启动拷贝为用户配置；「恢复默认」即重新取回此布局。
- **用户配置**：手机本地保存的格子网布局，编辑保存后立即生效。卸载重装后依赖系统自动备份恢复，不依赖重新编译或重装前手动操作。上限 20 条为 2026-09-22 升格（初定 18，同日改 4 列布局时调整为 20），旧 9 条配置原样生效（历史上因 9 上限截断的数据不可追回）。
- **格子编辑**：长按格子弹出的修改入口，可改标题与 URL。（2026-09-22 起：不再支持「全留空保存=清空」，清空格子一律走删除条目。）
- **创建条目**：新增一个格子条目的操作（决策 2026-09-22：不引入图片图标；入口有二——长按网格外留白区弹新建框〔落位：自动填第一个空格子，无洞才追加末尾〕、长按空格子弹编辑框〔定点创建〕；条目数上限 20，满员后留白长按提示已满。「留白区」= 格子网下方滚动可见的列表留白，非独立控件）。
- **删除条目**：把一个格子条目变为空格子的操作（决策 2026-09-22：就地留洞，其他格子位置不动，补齐靠拖拽排序；入口在格子编辑框内，带二次确认，确认文案显示条目名）。
- **层（WebView 层）**：宿主页面的一个实例，承载一个页面的浏览。层可叠放，最新一层盖在上方；返回键优先消化本层内部历史，退空后关闭本层、露出下一层。层数无上限（决策 2026-09-23）。
- **开层**：在当前层之上新建一层展示某 URL 的动作。两条路径殊途同归：主框架 http/https 自然导航自动开层；页面经桥主动调用 openHtmlPage 开层。
- **父页面**：开层动作发生时用户所在的页面，即关闭新层后回到的位置。
- **分层导航**：导航的分派规则——跨入互联网（http/https 主框架导航）即开层；本地资源（file 等网页协议）在本层加载；iframe 子框架导航丢弃；其余私有 scheme 尝试唤起外部应用，唤不起则忽略。
